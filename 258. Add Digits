public class Solution {
    public int addDigits(int num) {
        
        if(num < 10){
            return num;
        }
        return num % 9==0 ? 9 : num % 9;

        // int sum = 0;
        // if(num < 10){
        //     return num;
        // }
        // while(num > 0){
        //     sum += num < 10 ? num : num%10;
        //     num = num==10 ? 1 : num/10;
        //     if(num == 0 && sum/10 > 0){
        //         num = sum;
        //         sum = 0;
        //     }
        // }
        // return sum;
    }
}
